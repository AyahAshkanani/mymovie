
import { MovieWrapper } from "../Styles";
import { observer } from "mobx-react";

const MovieItem = () => {
    return (
        <MovieWrapper>
            <p>hello</p>
        </MovieWrapper>
    )

};




export default observer(MovieItem);